import 'package:flutter/material.dart';

class LegendListScreen extends StatelessWidget {
  const LegendListScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Coming soon...!'),
    );
  }
}
